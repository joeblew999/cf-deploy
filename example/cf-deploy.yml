# cf-deploy.yml — Config for the cf-deploy example worker.

worker:
  name: cf-deploy-example
  domain: gedw99.workers.dev
  dir: .

urls:
  production: https://cf-deploy-example.gedw99.workers.dev

github:
  repo: joeblew999/cf-deploy

version:
  source: package.json

output:
  versions_json: public/versions.json
